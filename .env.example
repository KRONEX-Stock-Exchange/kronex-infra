# == API SERVER
# PORT
SERVER_PORT=3000

# == REALTIME SERVER
REALTIME_SERVER_PORT=3001

# == FRONTEND
FRONTEND_PORT=80
# Vite variables are embedded in the frontend bundle at image build time.
# Use public URLs that are reachable from the user's browser in production.
VITE_API_BASE_URL=http://localhost:3000
VITE_REALTIME_URL=http://localhost:3001

# JWT
ACCESS_TOKEN_SECRET=
REFRESH_TOKEN_SECRET=

DATABASE_URL=mysql://root:1234@mysql:3306/stock
RABBITMQ_URL=amqp://admin:admin123@rabbitmq:5672

# == ENGINE
# MySQL (projection DB)
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=1234
MYSQL_DATABASE=stock

# RabbitMQ
RABBITMQ_HOST=127.0.0.1
RABBITMQ_PORT=5672
RABBITMQ_MGMT_PORT=15672
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=admin123

# WAL / snapshot
WAL_DIR=./data/wal
SNAPSHOT_INTERVAL=5m
